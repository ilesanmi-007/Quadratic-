#include <iostream>
#include "quadratic.h"
using namespace std;

int main()

{

	//Quadratic QuadRealRepeat(1,8,16); //test for same
	//Quadratic QuadRealDiff(1,8,1); //tests for diff val
	//Quadratic QuadImg(1,2,3); //this test for imaginary result

	//TESTED AND CONFIRMED:
	/*QuadRealRepeat.SetCoefficient();
	QuadRealRepeat.GetCoefficient();
	
	QuadRealRepeat.WriteRoots();*/


	//note: if i make use of the overloaded constructor,
	//there isnt need calling other fuction cause its been 
	//designed to automatically solve and display result.

	//but if i choose to follow the other of SETCOEFFICIENT,
	//THEN, it is necessary to call the Write root to print result.
	return 0;
}
